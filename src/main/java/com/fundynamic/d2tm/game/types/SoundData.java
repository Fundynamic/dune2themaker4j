package com.fundynamic.d2tm.game.types;

import org.newdawn.slick.Sound;

/**
 * Sound data holds information about a sound file and a potential grouping of sounds so that these can easily be
 * randomised. These 'groups' are predefined in an enum.
 */
public class SoundData {
    public Sound sound;
}
